# Build output
bin/
obj/

# EPiServer/Optimizely build artifacts: the CMS build targets unpack shell modules
# (CMS, Shell, Settings, Profile, TinyMce, VisitorGroups, ...) into this folder during build.
# Anchored to the project root so it does NOT match the packaged source module.config under
# contentFiles/any/any/modules/_protected/, which must be committed and shipped in the package.
/modules/

